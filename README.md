# How to implement CRUD operations in SvelteKit

## Steps to duplicate

- Run `npm init svelte` to create a SvelteKit Skeleton project with TypeScript enabled
- Run `npm install uuid svelte-icons`
- Run `npm i --save-dev @types/uuid`
